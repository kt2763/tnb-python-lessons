# #16 importと階層分け

動画: https://youtu.be/JUMELhLvlkY

`import` の基本と、複数ファイル・パッケージにコードを分割する方法、`__init__.py` によるパッケージのまとめ方を扱います。

- `import-0/` — 同じディレクトリにある `utils.py` を `main.py` からインポートする基本例
- `import-1/` — `mygame` パッケージ（`player.py` / `enemy.py`）を `__init__.py` でまとめ、`main.py` から短く読み込む例
